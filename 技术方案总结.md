# Simple-RV-Plus 技术方案总结

> 适用于结题报告

---

## 一、项目概述

Simple-RV-Plus 是一款基于 C++17 实现的 RISC-V RV32I 指令集模拟器，采用 **5 级双发射顺序流水线** 微架构，支持 64KB 物理内存空间、UART 串口外设、基础的同步异常处理机制。项目以裸机方式运行，配套 10 个汇编测试程序与 1 个 C 语言贪吃蛇游戏作为功能验证套件。

**技术指标**：模拟 RV32I 基础整数指令集全部 38 条指令（不含 FENCE 及 CSR 原子操作指令），理论峰值 IPC 为 2.0。

---

## 二、系统总体架构

系统采用面向对象的模块化设计，由以下 6 个核心组件构成：

```
┌──────────────────────────────────────────────────────┐
│                     main.cpp                          │
│              命令行入口 / 程序加载                      │
└──────────────────────┬───────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────┐
│                    CPUCore                            │
│   顶层调度：tick() → WB → MEM → EX → ID → IF          │
│   状态管理：PC / cycle_count / instr_count / running   │
└──┬──────────┬──────────┬──────────┬──────────────────┘
   │          │          │          │
┌──▼──┐ ┌────▼────┐ ┌───▼───┐ ┌───▼──────────┐
│Issue │ │Exec     │ │Memory │ │RegisterFile  │
│Unit  │ │Engine   │ │System │ │+ CSR         │
│ 译码  │ │ 执行/ALU│ │ 内存   │ │ 寄存器文件    │
│ 双发射│ │ 转发网络 │ │ MMIO  │ │              │
└──────┘ └─────────┘ └───────┘ └──────────────┘
```

各模块职责及对应源文件：

| 模块 | 头文件 | 源文件 | 职责 |
|------|--------|--------|------|
| 公共定义 | `common.h` | — | 类型别名、流水线结构体、CSR 地址宏 |
| 系统配置 | `SystemConfig.h` | — | 内存大小、初始 PC/SP/MTVEC |
| 流水线寄存器 | `PipelineRegisters.h` | `PipelineRegisters.cpp` | 四级流水线锁存器定义与刷新 |
| 寄存器文件 | `RegisterFile.h` | `RegisterFile.cpp` | 32 个 GPR + 4 个 CSR 读写 |
| 内存系统 | `MemorySystem.h` | `MemorySystem.cpp` | 64KB 物理内存 + MMIO 路由 |
| 发射单元 | `IssueUnit.h` | `IssueUnit.cpp` | 指令译码、双发射决策、冒险检测 |
| 执行引擎 | `ExecutionEngine.h` | `ExecutionEngine.cpp` | ALU 执行、四级转发网络 |
| CPU 核心 | `CPUCore.h` | `CPUCore.cpp` | 顶层流水线编排、陷阱处理 |

---

## 三、各模块详细技术设计

### 3.1 CPU 核心模块

#### 3.1.1 寄存器组设计

**通用寄存器 (GPR)**：`std::array<Word, 32>` 存储 x0–x31。x0 硬件硬连线为 0，`read_rs1` 对 rs=0 直接返回 0，`write_rd` 写入 x0 被硬件忽略。

为支持双发射流水线，寄存器文件提供：
- **4 个读端口**：`read_rs1`、`read_rs2`、`read_rs1_1`、`read_rs2_1`
- **2 个写端口**：`write_rd(rd, data, regWrite)`

**控制状态寄存器 (CSR)**：实现 4 个 M-Mode CSR，以专用成员变量存储：

| CSR | 地址 | 用途 |
|-----|------|------|
| `mstatus` | 0x300 | 机器模式状态（含全局中断使能 MIE） |
| `mtvec` | 0x305 | 陷阱向量基址 |
| `mepc` | 0x341 | 异常返回地址 |
| `mcause` | 0x342 | 异常原因编码 |

读写接口 `read_csr/write_csr` 采用 switch-case 实现，其余 8 个已定义未实现的 CSR 地址（如 `mie`、`mip`、`mtval` 等）预留扩展能力但不产生副效应。

#### 3.1.2 PC 与标志位设计

PC 是 `CPUCore` 中的 32 位 `Addr` 成员变量。更新路径有三条：
1. **顺序执行**：IF 阶段 `pc += 8`（双字取指）或 `pc += 4`（单字退化）
2. **分支/跳转**：EX 阶段分支命中或 JAL/JALR 执行时，PC 重定向至 `jump_target`
3. **陷阱重定向**：ECALL 时 PC 跳转至 `mtvec`，MRET 时 PC 从 `mepc` 恢复

无独立标志位寄存器。分支决策在 ALU 中直接比较两个源操作数，产生 `branch_taken` 布尔信号和 `jump_target` 地址。

#### 3.1.3 指令集实现

完整实现 RV32I 基础整数指令集，共 38 条指令，按指令格式分类：

| 指令格式 | 指令 | 译码入口 | 执行入口 |
|----------|------|----------|----------|
| R-Type | ADD, SUB, SLL, SLT, SLTU, XOR, SRL, SRA, OR, AND | op=0x33 | `executeRType()` |
| I-Type ALU | ADDI, SLTI, SLTIU, XORI, ORI, ANDI, SLLI, SRLI, SRAI | op=0x13 | `executeIType()` |
| I-Type Load | LB, LH, LW, LBU, LHU | op=0x03 | `executeIType()` → 地址计算，MEM 阶段完成读 |
| S-Type Store | SB, SH, SW | op=0x23 | `executeIType()` → 地址计算，MEM 阶段完成写 |
| B-Type Branch | BEQ, BNE, BLT, BGE, BLTU, BGEU | op=0x63 | `executeBranch()` |
| U-Type | LUI, AUIPC | op=0x37/0x17 | `executeIType()` |
| J-Type | JAL | op=0x6F | `executeJump()` |
| I-Type Jump | JALR | op=0x67 | `executeJump()` |
| System | ECALL, MRET, EBREAK | op=0x73 | EX 阶段（ECALL/MRET）/ WB 阶段（EBREAK） |

**未实现的 RV32I 指令**：FENCE、FENCE.I、CSRRW、CSRRS、CSRRC、CSRRWI、CSRRSI、CSRRCI。

**立即数译码**严格遵循 RISC-V 规范中 6 种立即数编码格式（I-type、S-type、B-type、U-type、J-type、shift-I-type），均正确实现符号扩展和位拼接。

#### 3.1.4 流水线主循环

流水线采用 **5 级双发射顺序** 架构，四级流水线锁存器定义如下：

```
IF_ID_Buffer  →  ID_EX_Buffer  →  EX_MEM_Buffer  →  MEM_WB_Buffer
  (2 slots)       (2 slots)        (2 slots)         (2 slots)
  + PC            + PC             + PC              + regWrite[2]
  + flush         + memRead[2]     + mem_addr[2]
                  + memWrite[2]    + mem_data[2]
                                   + memRead[2]
                                   + memWrite[2]
```

每个 `PipelineSlot` 封装原始指令字、译码信息（`DecodedData`）、执行结果和控制信号。

**流水线阶段执行顺序**：每个 `tick()` 中以 **WB → MEM → EX → ID → IF** 的**反向顺序**执行，确保硬件锁存器的正确行为——每个阶段从上一级锁存器读取输入，写入下一级锁存器。

**各阶段核心逻辑**：

| 阶段 | 函数 | 关键操作 |
|------|------|----------|
| IF | `fetch()` | 8 字节双指令预取，异常退化为 4 字节；槽位合并；流水线停顿检测 |
| ID | `decodeAndIssue()` | 解码 + 跨周期 Load-Use 检测 + `canIssueTogether()` 双发射决策 |
| EX | `execute()` | ALU 计算 + 四级转发 + 分支解析与冲刷 + 陷阱（ECALL/MRET） |
| MEM | `memoryAccess()` | 子字访存 + 符号扩展 + MMIO 路由 |
| WB | `writeBack()` | 寄存器写回 + EBREAK/非法指令检测 + 退休计数 |

**冒险处理机制总结**：

| 冒险类型 | 检测阶段 | 处理方法 |
|----------|----------|----------|
| RAW（ALU→ALU） | EX | 4 级转发：周期内 > EX_MEM > MEM_WB > RegFile |
| RAW（Load→Use） | ID（IssueUnit） | 1 周期停顿（气泡插入）+ 转发 |
| 结构冒险（内存双访问） | ID（`canIssueTogether`） | 禁止双发射，降级为单发射 |
| 控制冒险（分支） | ID + EX | 仅 slot 0 允许分支；命中时清空 IF_ID 和 ID_EX |
| 控制冒险（ECALL/MRET） | ID + EX | 单独发射；EX 阶段清空流水线 |

**双发射决策算法**（`canIssueTogether()`）按优先级检查：
1. 两条访存指令同时发射 → 拒绝（单端口内存）
2. 任一条是分支/跳转 → 拒绝（控制冒险简化）
3. slot 0 Load + slot 1 依赖 → 拒绝（Load-Use 无法转发）
4. 任一条是系统指令 → 拒绝（单独发射）
5. ALU→ALU RAW 依赖 → **允许**（转发网络解决）

### 3.2 内存与地址空间模块

#### 3.2.1 物理内存模拟

- **容量**：64KB（`std::vector<Byte>`，地址空间 0x00000000–0x0000FFFF）
- **字节序**：小端（Little-Endian），读写循环中以 `<< (8*i)` 和 `>> (8*i)` 逐字节组装
- **双指令预取**：`read_double_instr(Addr pc)` 使用 `std::memcpy` 从 `memory[pc]` 一次性复制 8 字节，用于 IF 阶段双发射取指
- **访存宽度**：`read(addr, n)` 和 `write(addr, n, data)` 的 `n` 参数支持 1/2/4/8 字节

#### 3.2.2 地址检查与越界

所有内存访问在 `read/write` 入口处检查 `addr + n > MEMORY_SIZE`，越界时抛出 `std::runtime_error("Memory read/write access out of bounds")`，由 `main.cpp` 捕获并报告。

子字访存（LB/LH/LW/LBU/LHU/SB/SH/SW）在 MEM 阶段根据 `funct3` 字段确定访问宽度，并对有符号加载（LB/LH）执行符号扩展到 32 位。

#### 3.2.3 程序加载

`load_program(filename)` 以二进制模式打开文件，从 `memory[0]` 开始最多读取 32KB（0x8000 字节）到内存。**不支持 ELF 格式**，需使用 `riscv64-unknown-elf-objcopy -O binary` 将 ELF 转换为原始二进制。

#### 3.2.4 MMU 与分页

**未实现**。采用平坦物理地址空间，无虚拟地址转换、无页表、无 TLB。

### 3.3 中断与异常模块

#### 3.3.1 同步异常支持

实现了三种 RISC-V 规范定义的同步异常/系统指令：

**ECALL（环境调用）**：
- 触发条件：EX 阶段检测 `d.is_ecall == true`（机器码 `funct12 == 0x000`）
- 处理流程：
  1. 保存 `mepc ← pc + 4`（异常返回地址）
  2. 设置 `mcause ← 11`（Environment call from M-mode）
  3. PC 跳转至 `mtvec`（陷阱向量地址）
  4. 清空 IF_ID、ID_EX 流水线锁存器
  5. 清理 EX_MEM 中异常指令的写回使能

**MRET（机器模式返回）**：
- 处理流程：
  1. PC ← `mepc`（恢复异常返回地址）
  2. 清空全部流水线锁存器

**EBREAK（断点）**：
- 在 WB 阶段检测（机器码 `0x00100073`），设置 `running = false` 停止模拟，输出最终 IPC 和寄存器状态

**非法指令检测**：WB 阶段检测全零机器码 `0x00000000`（通常由未初始化内存产生），报告致命错误并停止。

#### 3.3.2 中断生成（未实现）

- 无定时器中断（缺少 `mtime`/`mtimecmp` 寄存器）
- 无外部中断引脚
- 无软件中断机制
- `mip` 寄存器永不置位
- `mstatus.MIE` 位虽可存储，但从未被硬件检查（不产生中断，不需要检查）

#### 3.3.3 CSR 指令（未实现）

6 条 CSR 原子操作指令（CSRRW、CSRRS、CSRRC、CSRRWI、CSRRSI、CSRRCI）未在 `IssueUnit::decodeInstruction()` 中实现。`RegisterFile` 虽提供 CSR 读写基础方法，但指令译码器不会为 `opcode=0x73` 且 `funct3≠0` 的情况生成控制信号。这意味着软件无法通过标准 RISC-V 指令配置 CSR。

### 3.4 外设接口模块

#### 3.4.1 UART 串口（唯一外设）

MMIO 地址映射：

| 地址 | 方向 | 功能 |
|------|------|------|
| `0x10000000` | 写 | TX 发送数据寄存器 → `std::cout` 输出到控制台 |
| `0x10000000` | 读 | RX 接收数据寄存器 → 从主机键盘非阻塞读取 |
| `0x10000005` | 读 | LSR 线路状态寄存器：Bit0=RX就绪，Bit5=TX空闲 |

跨平台键盘捕获实现：
- **Windows**：`_kbhit()` 检测按键 + `_getch()` 读取字符
- **POSIX（Linux/macOS/WSL）**：`tcgetattr/tcsetattr` 关闭终端规范模式 + `O_NONBLOCK` 非阻塞 `read()`
- 全局静态缓冲区 `buffered_char` + `has_buffered_char` 标志位，确保字符被 CPU 读取后才允许新按键

#### 3.4.2 定时器中断

**未实现**。

#### 3.4.3 统一硬件接口规范

MMIO 路由通过 `MemorySystem::read/write` 中的地址范围判断实现：
- 地址 < 64KB：访问物理内存
- 地址 ≥ 64KB 且匹配外设地址：路由至对应外设（目前仅 UART）

扩展新外设只需在 `read/write` 中增加地址判断分支。

### 3.5 软件层实现

#### 3.5.1 裸机运行环境

| 配置项 | 值 | 说明 |
|--------|-----|------|
| PC 初始值 | 0x00000000 | 程序入口 |
| SP 初始值 | 0xFFF0 | 64KB 顶部 -16 字节 |
| MTVEC 初始值 | 0x00003000 | 陷阱向量（12KB 偏移处） |
| 程序加载位置 | 0x00000000 | 覆盖式加载 |

#### 3.5.2 汇编测试程序套件（10 个）

| 测试程序 | 测试目标 | 关键技术点 |
|----------|----------|------------|
| `echo.s` | UART 回显 | 轮询 LSR、读取 RX、写入 TX |
| `hello_uart.s` | 字符串输出 | 循环访存打印字符串 |
| `fib_test.s` | 计算与存储 | RAW 转发、循环控制、SW 存储 |
| `pipeline_stress.s` | 流水线压力 | 四阶段综合：RAW/Forwarding、Load-Use/Stall、分支冲刷、双发射并发 |
| `isa_complete_test.s` | ISA 完整性 | LUI/AUIPC、移位、子字访存、JAL/JALR 冲刷 |
| `test_trap.s` | 异常处理 | ECALL 进入 ISR → MRET 返回 |
| `test_arit_uart.s` | 算术 + 输出 | 立即数运算 + UART 循环打印 0-9 |
| `test_binary.s` | 分支测试 | BEQ 等值比较，UART 打印结果 |
| `test_cycle.s` | 周期测试 | 循环输出 0-9 验证周期计数 |
| `test_mem.s` | 内存测试 | SW/LW 比较验证内存读写一致性 |
| `int_test.s` | 中断配置 | 尝试配置定时器中断（**因 CSR 指令缺失无法运行**） |

#### 3.5.3 C 程序（贪吃蛇游戏）

全裸机贪吃蛇游戏，以 `riscv64-unknown-elf-gcc -O1 -march=rv32i -mabi=ilp32 -nostartfiles -nostdlib` 编译。软件层自实现了以下缺失支持：

- **UART 驱动**：`uart_putchar()`、`uart_kbhit()`、`uart_getchar()`
- **屏幕控制**：`clear_screen()` 通过 ANSI 转义序列实现
- **软件运行时函数**：`__mulsi3`（乘法）、`__divsi3`（除法）、`__modsi3`（取模）、`__udivsi3`（无符号除）、`__umodsi3`（无符号取模）——RV32I 无硬件乘除指令
- **伪随机数**：XORshift PRNG（`simple_rand()`）
- **输入缓冲**：环形缓冲区键盘队列（`push_key/pop_key`），含去重逻辑
- **延迟**：`delay(40000)` 忙等待循环
- **退出**：`ebreak` 指令正常终止

#### 3.5.4 构建工具链

- 交叉编译器：`riscv64-unknown-elf-gcc`
- 链接器脚本 `link.ld`：`.text @ 0x0000`，后接 `.rodata`、`.data`、`.bss`
- 编译流程：汇编/C → `.o` → ELF → `objcopy -O binary` → `.bin`

---

## 四、设计决策与技术亮点

### 4.1 优势
1. **完整的 RV32I 指令实现**：除 FENCE 和 CSR 指令外全部实现，功能验证充分
2. **真实流水线模拟**：5 级流水线 + 双发射 + 全套冒险处理，非简单指令解释器
3. **四级转发网络**：周期内（slot0→slot1）、EX→EX、MEM→EX、寄存器文件，最大化指令吞吐
4. **跨平台设计**：Windows 与 POSIX 双平台编译运行，UART 键盘输入使用条件编译
5. **丰富的测试套件**：10 个汇编测试覆盖流水线压力、ISA 完整性、异常处理等关键场景
6. **C 语言应用验证**：全功能贪吃蛇游戏作为系统级功能验证

### 4.2 局限与待改进
1. **CSR 指令缺失**：无法通过 RISC-V 标准指令配置中断/异常子系统
2. **无中断生成机制**：定时器中断、外部中断均未实现
3. **无 MMU**：无虚拟内存支持，无法对接需要分页机制的操作系统
4. **无分支预测**：采用假设不命中 + 命中冲刷策略，分支开销为 2 周期
5. **内存限制**：64KB 固定内存，无缓存层次结构
6. **无 RISC-V 特权模式**：M-Mode 仅部分实现，无 S-Mode/U-Mode

---

## 五、总结

Simple-RV-Plus 实现了一个功能完备的 RV32I 超标量顺序模拟器，覆盖了从取指、译码、双发射、执行转发、访存到写回的完整 5 级流水线，并对数据冒险（RAW）、结构冒险、控制冒险分别实现了转发、停顿与冲刷机制。软件层通过裸机汇编和 C 程序进行了多维度功能验证。

项目的核心价值在于**以相对简洁的代码量（约 850 行 C++）展现了超标量流水线微架构的完整实现**，适合作为 RISC-V 处理器微架构教学与基础研究的参考平台。后续可在 CSR 指令支持、中断子系统、MMU 分页等方向继续扩展。
