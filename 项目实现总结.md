# Simple-RV-Plus 项目实现总结

---

## 1. CPU 核心模块

### 寄存器组

- 32 个通用寄存器 (x0–x31)，x0 硬连线为 0
- 4 个读端口、2 个写端口，支持双发射同时读写
- CSR 寄存器：`mstatus` (0x300)、`mtvec` (0x305)、`mepc` (0x341)、`mcause` (0x342)，通过 `RegisterFile::read_csr/write_csr` 访问

### PC 与标志位

- PC 为 32 位 `uint32_t`，初始值为 `config.pc_init` (默认 0x00000000)
- 取指阶段每次递增 +8（双字取指）或 +4（单字退化）；分支/JAL/JALR/ECALL/MRET 在 EX 阶段重定向 PC
- 无独立标志位寄存器，分支决策直接在 ALU 中比较操作数产生 `jump_target` 和 `branch_taken` 信号

### 基础指令集（约 38 条）

实现了 RV32I 整数指令集的全部指令：

| 类型 | 指令 |
|------|------|
| R 型 | ADD, SUB, SLL, SLT, SLTU, XOR, SRL, SRA, OR, AND |
| I 型 (ALU) | ADDI, SLTI, SLTIU, XORI, ORI, ANDI, SLLI, SRLI, SRAI |
| I 型 (访存) | LB, LH, LW, LBU, LHU |
| S 型 | SB, SH, SW |
| B 型 | BEQ, BNE, BLT, BGE, BLTU, BGEU |
| U 型 | LUI, AUIPC |
| J 型 | JAL, JALR |
| 系统 | ECALL, MRET, EBREAK |

**未实现**：FENCE、FENCE.I 以及 6 条 CSR 原子操作指令 (CSRRW/CSRRS/CSRRC/CSRRWI/CSRRSI/CSRRCI)

### 流水线主循环

- **5 级流水线**：IF → ID/ISSUE → EX → MEM → WB，顺序提交
- **双发射**：每周期最多发射两条指令，通过 `canIssueTogether()` 检查结构冒险（单端口内存）、控制冒险（分支仅限 slot 0）、Load-Use 冒险
- **转发网络**：4 级优先级——周期内转发（slot 0→slot 1）、EX→EX 转发、MEM→EX 转发、寄存器文件
- **Load-Use 停顿**：跨周期检测 ID_EX 中加载指令与 IF_ID 中依赖指令，插入 1 周期气泡
- **分支处理**：仅 slot 0 执行分支，命中即清空 IF_ID 和 ID_EX，假设不命中无预测器

---

## 2. 内存与地址空间模块

### 物理内存模拟

- 64KB 字节可寻址内存 (`std::vector<Byte>`，65536 字节)
- 小端字节序读写，支持 1/2/4/8 字节访问
- `read_double_instr()` 使用 `memcpy` 一次读取 8 字节供双发射取指
- 程序以原始二进制加载到 0x0000 起始地址（无 ELF 解析）

### 地址检查与越界

- 对 `addr + size > MEMORY_SIZE` 的所有读写抛出 `std::runtime_error`
- 子字访存（LB/LH/LW/LBU/LHU/SB/SH/SW）在 MEM 阶段完成符号扩展或截取

### MMU / 分页机制

- **未实现**。无虚拟内存、无页表、无 TLB、无地址转换。使用平坦物理地址空间

---

## 3. 中断与异常模块

### 中断控制器模拟

- **未实现**。无外部中断引脚、无软件中断机制、无定时器中断生成逻辑，`mip` 寄存器永不置位

### 异常入口、上下文保存与恢复

- **ECALL**：EX 阶段识别，PC+4 保存至 `mepc`，`mcause` 设为 11，PC 跳转至 `mtvec`，清空流水线
- **MRET**：EX 阶段识别，PC 从 `mepc` 恢复，清空流水线
- **EBREAK**：WB 阶段识别 (机器码 0x00100073)，停止模拟器运行
- 非法指令 (0x00000000) 在 WB 阶段被检测并报致命错误
- 其他异常（指令地址未对齐、访问异常等）**未实现**
- CSR 指令（CSRRW 等）未在解码器中实现，因此软件无法通过指令直接读写 CSR

### 时钟中断

- **未实现**。无定时器硬件，无 `mtime`/`mtimecmp` 寄存器

---

## 4. 外设接口模块

### UART 串口

- **MMIO 地址**：TX 数据/状态 `0x10000000`，LSR 状态 `0x10000005`
- TX 写入 `0x10000000` → 通过 `std::cout` 输出到主机控制台
- RX 读取 `0x10000000` → 从主机键盘非阻塞读取（Windows 用 `_kbhit()`/`_getch()`，POSIX 用 `tcgetattr` + `O_NONBLOCK`）
- LSR 读取 `0x10000005` → 返回 TX 空闲位 (0x20) | RX 数据就绪位 (0x01)

### 定时器中断

- **未实现**

### 统一硬件接口规范

- 通过 `MemorySystem::read/write` 中的地址范围检查实现 MMIO 路由（地址 >= 64KB 落入外设空间）
- 易于扩展：在 `read/write` 中新增地址判断即可加入新外设

---

## 5. 软件层实现

### 裸机运行环境

- 无操作系统，程序在 0x0000 直接启动
- 栈指针 (x2) 初始化为 `0xFFF0`（64KB 顶部 -16）
- 陷阱向量 `mtvec` 初始化为 `0x00003000`

### 汇编测试程序（10 个）

| 文件 | 功能 |
|------|------|
| `echo.s` | UART 键盘回显 |
| `hello_uart.s` | Hello World 打印 |
| `fib_test.s` | 斐波那契数列前 20 项计算 |
| `test_trap.s` | ECALL/MRET 异常处理测试 |
| `int_test.s` | 定时器中断配置测试（因缺少 CSR 指令支持无法运行） |
| `pipeline_stress.s` | 流水线压力测试（RAW 转发、Load-Use、双发射） |
| `isa_complete_test.s` | U 型指令、子字访存、JAL/JALR 指令测试 |
| `test_arit_uart.s` | 算术 + UART 输出测试 |
| `test_binary.s` | 分支判断测试 |
| `test_cycle.s` | 循环输出测试 |
| `test_mem.s` | 内存存储/加载比较测试 |

### C 程序

`main.c`：全功能贪吃蛇游戏，自行实现了：
- UART 驱动（`uart_putchar`、`uart_kbhit`、`uart_getchar`）
- 软件除法/乘法/取模 (`__divsi3`/`__mulsi3`/`__modsi3` 等)
- XORshift 伪随机数生成器
- 环形缓冲区键盘队列
- 全部以 `-nostartfiles -nostdlib` 裸机编译

### 构建工具

- `compile_asm.sh`：汇编 → ELF → 原始二进制
- `compile_c.sh`：C 编译 + 链接脚本 `link.ld`（.text @ 0x0000）
- 交叉编译器：`riscv64-unknown-elf-gcc`，目标 `-march=rv32i -mabi=ilp32`

---

## 总结表

| 模块 | 状态 | 完成度 |
|------|------|--------|
| CPU 核心（寄存器/指令/流水线） | 完整 RV32I + 5 级双发射 | 高 |
| 内存与地址空间 | 64KB 平坦物理内存，无 MMU | 中 |
| 中断与异常 | ECALL/MRET 可用，无中断生成，无 CSR 指令 | 中低 |
| 外设接口 | 仅有 UART，无定时器 | 低 |
| 软件层 | 10 个汇编测试 + 1 个 C 游戏 | 高 |

### 核心缺失

- CSR 指令解码（导致无法通过软件配置中断）
- 定时器/中断生成逻辑
- MMU 分页机制
